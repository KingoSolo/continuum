# Runtime environment for the future API service.
# Add only non-secret defaults or variable names here.
NODE_ENV=development
PORT=3001

# CockroachDB connection used by the Prisma persistence layer.
DATABASE_URL="postgresql://username:password@host:26257/continuum?sslmode=verify-full"

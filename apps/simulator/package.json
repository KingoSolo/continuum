{
  "name": "@continuum/simulator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --project tsconfig.json --noEmit",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "echo 'No tests configured for @continuum/simulator'"
  },
  "devDependencies": {
    "@continuum/config": "workspace:*"
  }
}

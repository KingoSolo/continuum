NODE_ENV=development
API_BASE_URL=http://localhost:3001
# UUID of the seeded ARES-7 mission. The public API currently exposes domain actions,
# not a mission lookup endpoint, so this is supplied by the demo environment.
MISSION_ID=
# Keep this value constant to make repeated scenario inputs deterministic.
SIMULATOR_RUN_ID=demo-001
